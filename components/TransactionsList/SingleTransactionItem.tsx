import { SingleTransaction } from "@/types";
import { FC } from "react";
import { View } from "../Themed";

type Props = { transaction: SingleTransaction };

export const SingleTransactionItem: FC<Props> = () => {
  return <View></View>;
};
