import { RecurrentTransaction } from "@/types";
import { FC } from "react";
import { View } from "../Themed";

type Props = { transaction: RecurrentTransaction };

export const RecurrentTransactionItem: FC<Props> = () => {
  return <View></View>;
};
