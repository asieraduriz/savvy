import { toFormattedDate } from "./Date";
import {
  toGroupedByDateOccurences,
  toOccurrence,
  toOccurrences,
} from "./Occurrence";

export const Transformers = {
  toOccurrence,
  toOccurrences,
  toGroupedByDateOccurences,
  toFormattedDate,
};
